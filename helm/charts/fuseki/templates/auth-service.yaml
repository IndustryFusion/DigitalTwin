apiVersion: v1
kind: Service
metadata:
  name: fuseki-auth
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: fuseki-auth
  ports:
    - name: http
      port: 8080
      targetPort: 8080
