[pytest]
# Collect only the real test trees. Without this, pytest's default
# `*_test.py` pattern also imports tools/ttl_test.py -- a live-cluster
# release driver, not a test module -- which drags its unexecutable
# statements into the coverage gate.
testpaths = tests udf
